#include <stdio.h>

int main()
{
    char alpha;
    printf("Enter a character: ");
    scanf("%c", &alpha);
    printf("You entered %c.\n", alpha);
    printf("ASCII value is %d.\n", alpha);
    return 0;
}
